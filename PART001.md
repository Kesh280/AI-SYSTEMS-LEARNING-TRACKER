# --PRINT STATEMENT--

- We used print statement to print the statements we needs to.

- We can write multiple values in print statement.

- print("Hey" ,6 ,7 , sep="~" , end="007") in this code we wrote a sep command by which we can seperate all things wrtten in the print by using the sign in double quotes.

- end is used to end the statement by using the thing we provide in the quotes.

# --COMMENTS--

- The comment is used to avoid the run of the statement we wrote in the code.

- We have several types of comments.

- Comments by hashtag,by triple single quote
'''like this'''

- we also write the comments in triple double quotes.

- We also have a shortcut to comment several lines in just one click "cmnd+/".

# --ESCAPE SEQUENCE CHARACTER--

- These are the characters used to insert things in between of the code.

- \n is used to start from a new line.

- \" and \' these are used to add quotes in the code.

# --VARIABLES--

- Variable is like a container that holds the data.

# --DATA TYPES--

- There are several types of data:-

- we can get to know the type by use "type()"in print statement.

- int --> integer
- float --> used for decimal values 
- complex --> for complex numbers
- str --> for string 
- bool --> true and false 
- list --> collection of different data types and it is mutable. We can make changes in it. We use index to access the value. 
- Tupple --> collection of elements. it is ordered.it is immutable. we cannot make changes in it. We access values by using index.
- dict --> collection of key-value pairs. we use key to access the values.

# --TYPECASTING--

- The conversion of any data type to another data type.
- It only tries if it is convertible only then it converts.

- Explicit Typecasting:- jo hum order deke krte hai.
- Implicit Typecasting:- Jo python auatomatically krta hai.








